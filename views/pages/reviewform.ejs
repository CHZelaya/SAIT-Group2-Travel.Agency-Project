<!-- CREATE TABLE `reviews` (
  `ReviewId` int NOT NULL AUTO_INCREMENT,
  `CustomerId` int NOT NULL,
  `Rating` tinyint NOT NULL,
  `Comments` varchar(255) NOT NULL,
  `SubmissionDate` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`ReviewId`),
  KEY `CustomerId` (`CustomerId`),
  CONSTRAINT `reviews_ibfk_1` FOREIGN KEY (`CustomerId`) REFERENCES `customers` (`CustomerId`),
  CONSTRAINT `reviews_chk_1` CHECK ((`Rating` between 1 and 5))
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb3 -->

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registration Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/static/css/style.css">
    <script src="/static/js/reviewscript.js" defer></script>
    <!-- needed to use defer to let html load fully so that js can work properly -->

</head>

<body>
    <!-- Navigation links header -->
    <div class="container">
        <header class="d-flex justify-content-center py-3">
            <img src="/mainpagepics/logo.png" alt="Logo" class="logo">
            <ul class="nav nav-pills">
                <li class="nav-item"><a href="/" class="nav-link">Home</a></li>
                <li class="nav-item"><a href="/vacation" class="nav-link">Vacation Packages</a></li>
                <li class="nav-item"><a href="/contact" class="nav-link">Meet the Team</a></li>
                <li class="nav-item"><a href="/register" class="nav-link" aria-current="page">Register Now</a>
                </li>
            </ul>
        </header>
    </div>

    <div class="container px-4 py-5">
    <div id="okmessage" class="alert alert-success thanks-rating" role="alert">
        Thanks for submit!
      </div>

    <h2 class="pb-2 border-bottom">Share Your Experience With Us</h2>

    <!-- Review Form -->
    <section id="review-form">
        <form action="/submit-review" method="POST" id="reviewForm">
                        
            <div class="mb-3">
                <label for="phone" class="form-label">Existing Registered Home Phone Number</label>
                <input type="tel" class="form-control" placeholder="10 digits (##########)" id="phone" name="CustHomePhone" maxlength="15" required>
                <p class="error" id="phoneDesc">Please enter a valid phone number</p>
            </div>
            
            <div class="mb-3">
                <label for="submissionDate">Submission Date</label>
                <input type="text" class="form-control" id="submissionDate" name="SubmissionDate" value="<%= new Date().toISOString().split('T')[0] %>" readonly>
            </div>
            
            <div class="mb-3">
                <label class="rating">Rating:</label>
                <div class="form-check form-check-inline">
                    <input class="form-check-input" type="radio" name="Rating" id="inlineRadio1" value="1">
                    <label class="form-check-label" for="inlineRadio1">Terrible</label>
                </div>
                <div class="form-check form-check-inline">
                    <input class="form-check-input" type="radio" name="Rating" id="inlineRadio2" value="2">
                    <label class="form-check-label" for="inlineRadio2">Poor</label>
                </div>
                <div class="form-check form-check-inline">
                    <input class="form-check-input" type="radio" name="Rating" id="inlineRadio3" value="3">
                    <label class="form-check-label" for="inlineRadio3">Average</label>
                </div>
                <div class="form-check form-check-inline">
                    <input class="form-check-input" type="radio" name="Rating" id="inlineRadio4" value="4">
                    <label class="form-check-label" for="inlineRadio4">Good</label>
                </div>
                <div class="form-check form-check-inline">
                    <input class="form-check-input" type="radio" name="Rating" id="inlineRadio5" value="5">
                    <label class="form-check-label" for="inlineRadio5">Excellent</label>
                </div>
                <p class="error" id="rateDesc">Please select one option</p>
            </div>
            
            <div class="mb-3">
                <label for="comments" class="form-label">Please share your experience:</label>
                <textarea class="form-control" rows="3" id="comments" name="Comments" maxlength="255" required></textarea>
                <p class="error" id="commentsDesc">Please enter your comment</p>
            </div>
    
            <button type="submit" class="btn btn-warning btn-lg">Submit Review</button>
            <button type="reset" class="btn btn-warning btn-lg" id="resetButton">Reset</button>
        </form>
    </section>

    
    <script>
        document.addEventListener('DOMContentLoaded', (event) => {
            const submissionDateInput = document.getElementById('submissionDate');
            const today = new Date().toISOString().split('T')[0]; // Format YYYY-MM-DD
            submissionDateInput.value = today;
        });
    </script>
    
    
    </div>
    </body>
    </html>

    